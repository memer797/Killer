<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
    #trending {
      background-color: transparent;
    }

    #trending .movie-container {
  margin: 10px;
  text-align: center;
background-color: transparent;
  width: 150px; /* Adjust the width as needed */
}

#trending .movie-image {
  width: 100%; /* Make the image fill the container */
  height: 32.68%; /*160px Adjust the height as needed */
object-fit: cover;
  border-radius: 10px;
}
.new-section .movies-container .movie-container .movie-image {
border-radius: 4px;
}

    .leftright-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px;
    }

.left-side {
      flex: 1;
    }

    .right-side {
      flex: 1;
      text-align: right;
    }
    
.see-all {
  color: #4285f4;
  text-decoration: none;
  font-size: 14px;
  transition: color 0.3s ease;
}

.see-all:hover {
  color: #e50914; /* Hover color */
}

.new-section {
  overflow-x: auto;
  white-space: nowrap;
  direction: ltr; /* Changed to left-to-right direction */
  text-align: left; /* Align the text to the left */
  margin-top: 3px; /* Adjust the top margin as needed */
  padding-left: 15px; /* Add padding to the left */
  padding-right: 15px; /* Add padding to the left */
}

.new-section .section-title {
      font-size: 22px;
      font-weight: bold;
      margin-top: 10px;
      margin-bottom: 10px;
      color: #fff; /* Set the text color */
}
.leftright-container .left-side .section-title {
      font-size: 16px;
      font-weight: bold;
      color: #fff;
}
.new-section .movie-container {
  display: inline-block;
  margin-right: 7px; /* Adjust spacing between movies */
  text-align: center;
  width: 135px; /* Set the width */
  height: auto;
}
 
.new-section .movie-image {
  width: 100%; /* Make the image fill the container */
  height: 80px; /* Set the height */
  object-fit: cover;
}

.new-section .movie-container .movie-name {
  display: flex;
  flex-deriction: row;
  margin-top: 5px;
  margin-bottom: 20px;
  font-size: 14px;
  color: #fff;
  overflow: hidden;
  overflow-y: auto;
  text-overflow: ellipsis;
  white-space: normal; /* Change from nowrap to normal */
}

.new-section .movie-name {
  margin-top: 5px;
  margin-bottom: 20px;
  font-size: 14px;
  color: #fff; 
  overflow-wrap: break-word;
  }
    
  </style>
</head>

<body>
<div class="taremaka">

  <div class="leftright-container">
    <div style = "font-size: 20px; font-weight: bold; color: #fff; margin-left: 6px; margin-top: -5px; margin-bottom: -5px;">TRENDING🔥🔥</div>
  </div>
  <div id="trending">

    <!-- Add more movie containers as needed -->
  </div>
</div>
</body>
</html>
