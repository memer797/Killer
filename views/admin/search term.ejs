<script>
console.log("<%= srarchTermRecord%>");
</script>

<% searchTermRecord.forEach(data => {
    var ndata = data.split("[•|{\\:/}|•]");
    if (ndata && ndata.length > 1) { %>
        <%= console.log(ndata); %>
        <%= ndata[0] %> => <%= ndata[1] %>
<%   }
}); %>

